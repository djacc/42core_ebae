/*Allocates (with malloc(3)) and returns a substring
from the string ’s’.
The substring begins at index ’start’ and is of
maximum size ’len’.*/

#include <stdlib.h>


char *ft_substr(char const *s, unsigned int start, size_t len)
{
	// malloc new string size from start till len or NULL
	// assign new string to start pos and assign until len or NULL
	// if alloc fails: does that mean if len is > than strlen from start?
	int		pos;
	char	*new_s;
	
	if (*new_s)
		return (NULL);
	
	
	
	return (NULL);
}